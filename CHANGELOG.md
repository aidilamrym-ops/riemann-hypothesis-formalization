# Changelog — ALMIGHTY-FRAMEWORK (Riemann Hypothesis Formalization)

All entries below are backed by disk NYATA (`exports/transpilation/**`, ordered GitHub Actions API
verdicts). Nothing is backdated narration.

## [v2.0.0] — Strict YAML Anti-Shadow Verification Gate (commit 89ca5a8, 2026-09-24)
### Fixed (root-cause, ada bukti API NYATA)
- **Workflow file issue AKAR CI**: step-name validator `ANTI-COCOKLOGI: baca artefak disk NYATA`
  mengandung ` : ` (colon-spasi) di plain scalar YAML tanpa kutip → `external-audit.yml` **INVALID**;
  GitHub menyatakan verdict NYATA "This run likely failed because of a workflow file issue" — bukan
  validator yang menolak. Fix: buang colon di step-name (kutip) → YAML `STRICT=0`, parse OK.
- **Anti-shadow step**: validator konsistensi kini satu step MANDIRI (1 `run:`), tribunal Z3 step
  TERPISAH — validator **tidak** lagi bisa ditimpa oleh `run:` kedua dalam satu step.
### Added
- `transpilation_manifest.json` (schema v2) — metrics di-lock dari nexus NYATA (66/1880/`log_one_plus_lt`).
- Verdict tribunal jujur: F1/F2/F4 `UNSAT`; **F3 `UNKNOWN` (transcendental spectral rigidity) dibiarkan
  UNKNOWN** — batch ini TIDAK pernah di-polish jadi klaim (anti-cocoklogi).

## [v1.3.1] — Nexus anti-cocoklogi dari AST korpus nyata (2026-09-23)
- Flagship `log_one_plus_lt` di-deteksi dari scan AST LIVE (66 modul), bukan hardcoded.
- 4 backbone (dki/v/thy/c) di-generate flagship `log_one_plus_lt` — artefak lintas-backbone konsisten.

## [v1.x] — Era Hardcoded Lipstik (SEBELUM anti-shadow gate; KENANG JUJUR)
- `v1.0-v1.2`: transcompilation & validator meningkatkan bombastik teks saat CI pipeline bermasalah.
  Versi lama ini **DITOLAK** oleh tribunal anti-cocoklogi (honest: cocoklogi era v1 = alasan gate v2 dibuat).

## [v0.x] — Seed (2026-02)
- Pustaka awal coraci (induksi AH, estetika Z3) tanpa gate konsistensi lintas-backbone.